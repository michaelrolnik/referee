title: Referee
description: A C++ compiler toolchain for the REF requirement-verification language — ANTLR4 front-end, LLVM IR back-end.
theme: jekyll-theme-cayman

# Absolute-URL base for the GitHub Pages project site (michaelrolnik.github.io/referee).
# Needed so sitemap.xml `absolute_url` and theme asset paths resolve correctly.
url: https://michaelrolnik.github.io
baseurl: /referee
