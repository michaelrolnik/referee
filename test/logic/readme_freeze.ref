# Extracted verbatim from README.md (the freeze-variable example), with the
# declarations it relies on. Kept as a fixture so the example cannot rot.
data problem : boolean;
data alarm   : boolean;

// A problem must be followed by an alarm within 5 seconds
G(problem => t@(F(alarm && __time__ - t.__time__ <= 5)));
